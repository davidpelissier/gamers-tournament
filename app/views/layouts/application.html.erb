<!DOCTYPE html>
<html>
<head>
  <title>GamersTournament</title>

  <link href='https://fonts.googleapis.com/css?family=Lato:300,400,700' rel='stylesheet' type='text/css'>
  <link href='https://fonts.googleapis.com/css?family=Raleway:300,400,700' rel='stylesheet' type='text/css'>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">
          <i class="icon-crown"></i>
          <strong>Gamers</strong>Tournament
        </a>
      </div>

      <div class="collapse navbar-collapse" id="navbar-collapse">
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <% if current_user.teams.empty? %>
            <li>
              <%= link_to raw("<span class='glyphicon glyphicon-left icon-users'></span>Rejoindre une équipe"), new_user_registration_path %>
            </li>
            <% else %>
            <li>
              <%= link_to raw("<span class='glyphicon glyphicon-left icon-users'></span>Mes équipes"), new_user_registration_path %>
            </li>
            <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <span class="glyphicon glyphicon-user glyphicon-left"></span> 
              Mon compte <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profil", edit_user_registration_path %></li>
              <li><%= link_to "Préférences", '#' %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Se déconnecter", destroy_user_session_path, :method => :delete %></li>
            </ul>
          </li>
          <% else %>
          <li>
            <%= link_to raw("<span class='glyphicon glyphicon-log-in glyphicon-left'></span>Se connecter"), new_user_session_path %>
          </li>
          <li>
            <%= link_to raw("<span class='glyphicon glyphicon-plus glyphicon-left'></span>Créer un compte"), new_user_registration_path %>
          </li>
      	  <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="jumbotron"></div>

  <div class="main">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <% if notice %>
          <div class="alert alert-info" role="alert" id="notice">
            <%= notice %>
          </div>
          <% end %>
          <% if alert %>
          <div alert="alert alert-error" role="alert" id="notice">
            <%= alert %>
          </div>
          <% end %>
        </div>
      </div>

      <%= yield %>
    </div>
  </div>

  <footer class="footer">
    <div class="container">
      <p class="text-muted text-right">
        © 2016 GamersTournament<br />
        © 2016 Quentin DOUZIECH & David PELISSIER
      </p>
    </div>
  </footer>

</body>
</html>
