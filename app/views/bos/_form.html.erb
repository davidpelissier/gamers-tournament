<%= form_for(@bo) do |f| %>
  <% if @bo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bo.errors.count, "error") %> prohibited this bo from being saved:</h2>

      <ul>
      <% @bo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phase %><br>
    <%= f.text_field :phase %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
