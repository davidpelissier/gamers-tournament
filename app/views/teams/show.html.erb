<p>
  <strong>Name:</strong>
  <%= @team.name %>
</p>

<p>
  <strong>Location:</strong>
  <%= @team.location %>
</p>

<p>
	<strong>Membres:</strong>
	<table class="table table-hover">
		<thead>
			<tr>
				<td>Username</td>
				<td>Action</td>
			</tr>
		</thead>
		<tbody>
		<% @team.users.each do |user| %>
			<tr>
				<td><%= user.username %></td>
				<td><%= link_to "Partir", delete_user_from_team_path(@team.id) %></td>
			</tr>
		<% end %>
		</tbody>	
	</table>
</p>

<%= link_to 'Back', teams_path %>
