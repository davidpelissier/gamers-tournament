<h1>Liste des équipes</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Pays d'origine</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= team.location %></td>
        <td><%= link_to 'Voir', team %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
